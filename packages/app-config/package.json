{
  "name": "app-config",
  "version": "0.0.1",
  "main": "./build/index.js",
  "types": "./src/index.ts",
  "scripts": {
    "clean": "rm -rf build",
    "build_config": "tsup --clean",
    "debug_config": "tsup --watch"
  },
  "files": ["build", "src"],
  "devDependencies": {
    "typescript": "5.7.2",
    "tsup": "^8.0.1",
    "typescript-config": "*"
  }
}
