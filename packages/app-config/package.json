{
  "name": "app-config",
  "version": "0.0.1",
  "main": "./build/index.js",
  "types": "./src/index.ts",
  "scripts": {
    "clean": "rm -rf build",
    "build_config": "tsup --clean",
    "debug_config": "tsup --watch",
    "test": "jest",
    "test:watch": "tsup --watch",
    "dev": "tsup --watch"
  },
  "files": [
    "build",
    "src"
  ],
  "dependencies": {
    "zod": "^3.25.64"
  },
  "devDependencies": {
    "tsup": "^8.5.0",
    "typescript": "5.7.2",
    "typescript-config": "*"
  }
}
